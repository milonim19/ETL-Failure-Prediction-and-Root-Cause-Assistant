# =============================================================================
# ETL Failure Prediction & Root Cause Assistant — Python dependencies
# =============================================================================
#
# Scope: FastAPI backend (`backend_api/`), ML pipeline (`ml/`), training,
#        evaluation, and SHAP-based explanations.
#
# Recommended: Python 3.11.x (tested with 3.11.8)
#
# Install (from this repository root, next to `backend_api/` and `ml/`):
#   python -m venv .venv
#   .venv\Scripts\activate          # Windows
#   pip install -r requirements.txt
#
# Run API:
#   uvicorn backend_api.main:app --host 0.0.0.0 --port 8000
#
# Frontend (React/Vite) uses Node.js — see `package.json`
#
# Core libraries (direct): fastapi, uvicorn, pydantic, joblib, pandas, numpy,
#                          scikit-learn, shap
# Other lines: transitive dependencies, pinned for reproducible installs.
# =============================================================================

annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
click==8.3.3
cloudpickle==3.1.2
colorama==0.4.6
fastapi==0.136.1
h11==0.16.0
idna==3.13
joblib==1.5.3
llvmlite==0.47.0
numba==0.65.1
numpy==2.4.4
packaging==26.2
pandas==3.0.2
pydantic==2.13.3
pydantic_core==2.46.3
python-dateutil==2.9.0.post0
scikit-learn==1.6.1
scipy==1.17.1
shap==0.47.2
six==1.17.0
slicer==0.0.8
starlette==1.0.0
threadpoolctl==3.6.0
tqdm==4.67.3
typing-inspection==0.4.2
typing_extensions==4.15.0
tzdata==2026.2
uvicorn==0.46.0
